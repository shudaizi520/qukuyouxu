# 更新记录

## 1.4.3 - 2026-09-20

- “我的歌单”改为真正的全屏音乐工作区：顶栏、歌单侧栏、内容标题与底部播放器保持固定，只有歌单列表、歌曲列表和内嵌工具内容各自滚动。
- 顶栏改为全库搜索，可直接播放搜索结果并加入已有受管歌单；歌单、搜索、创建工具、运行状态和设置在同一页面切换，播放不会因切换界面中断。
- 播放器拆成独立模块并取消音频元数据预加载，避免一次点击触发相邻的重复 Plex 转码请求；首次启动失败会自动重试一次，仍失败才在底栏显示操作。
- 播放请求使用代际标识隔离，快速换歌时上一首迟到的失败不会覆盖当前歌曲；Plex 存在多个安全音频版本时优先使用选中版本，否则按服务器顺序稳定选择。
- 工作区、全库搜索和播放器分别由独立模块维护，页面入口只负责账户与歌单编排；视觉尺寸全部使用语义变量，为后续壁纸和皮肤切换保留统一接口。

## 1.4.2 - 2026-09-20

- “我的歌单”改为固定账户与工具区、独立滚动歌单区和紧凑歌曲列表；歌单切换保持页面尺寸稳定，底部播放器缩为单行并在原处显示失败与重试。
- 修复切换 Plex 用户或音乐资料库后仍显示上一个账户歌曲的问题；快速连续切换也会丢弃过期响应，不再串号。
- 播放队列与当前浏览歌单分离，切换歌单、进入创建工具或删除其他歌单时不再误停正在播放的歌曲。
- 浏览器无法直接播放的 FLAC 等格式由 Plex 实时转为 MP3，原始音乐文件不修改；封面、歌名、歌手、进度、音量与连续播放统一保留在底栏。
- 每日推荐在首次发布前也保留唯一入口，不再同时显示两个“每日推荐”，也不会因尚未创建而从首页消失。
- 外部歌单整理为紧凑导入栏、来源切换和结果区，嵌入首页时自动适应内容高度，试听失败显示在对应歌曲旁边。

## 1.4.1 - 2026-09-20

- 修复恢复数据后安装标识变化导致旧版受管歌单被误报“助手管理标记缺失”；只有歌单编号、名称、成员和已保存指纹全部一致时才迁移标记，普通同名歌单仍不会被接管。
- 首页会先显示完整歌单列表，再逐个打开可用歌单；某一个旧歌单需要核对时，不再导致侧栏数量归零或遮住其他正常歌单。
- 每日推荐、智能歌单、分类歌单与外部导入歌单统一使用恢复后的所有权校验；标记迁移只更新摘要，不改变歌曲、顺序、封面或音乐文件。

## 1.4.0 - 2026-09-20

- 首页升级为统一“我的歌单”：每日推荐、智能歌单、分类歌单和外部歌单按当前 Plex 用户与音乐库集中展示，不再分散到多个顶层页面。
- 新增连续播放队列和固定底部播放器，可从歌单任意歌曲开始播放，自动续播下一首，并直接显示 Plex 曲库已有封面、歌名、歌手和进度。
- 支持搜索当前 Plex 曲库并把歌曲加入任一受管歌单，也可从歌单手动移除歌曲；个人增删会作为独立调整保存，后续自动更新继续尊重这些选择。
- 每个试听与封面请求都会重新校验当前用户、音乐库、受管歌单及曲目归属；浏览器不会取得 Plex Token，也不能借接口读取歌单之外的音频。
- 每日推荐、智能歌单、外部歌单和曲库整理集中到首页侧栏的“创建与整理”，在同一工作区打开，播放不会因进入创建页面而中断。
- 外部歌单页面精简为“导入歌单、创建 Plex 歌单、缺失歌曲”三块；试听控制和进度回到歌曲行内，不再需要滚到页面底部暂停。

## 1.3.1 - 2026-09-20

- 修复 QQ 音乐客户端生成的 `c6.y.qq.com` 公开分享短链接无法导入；现在会在逐跳校验 HTTPS、公网 DNS 和 QQ 官方域名后解析真实歌单编号，其他跳转仍会拒绝。

## 1.3.0 - 2026-09-20

- 新增“外部歌单”：支持 QQ 音乐公开歌单、QQ 排行榜、网易云音乐公开歌单，以及 M3U、TXT、CSV 文件；导入结果按 Plex 用户和音乐资料库独立保存。
- 使用保守的多阶段匹配，分开展示可靠匹配、需要确认和曲库缺失；只有可靠匹配或用户确认的本地歌曲才可发布，避免同名、翻唱、现场版和多歌手作品误配。
- 外部歌单发布沿用助手所有权保护，只更新本软件创建的对应 Plex 歌单；修改、删除和来源更新均校验账户、服务器、曲库与歌单身份，不碰用户手工歌单。
- 缺失歌曲可按 QQ 音乐或网易云音乐格式导出，方便复制到官方客户端搜索和补歌；这只是生成清单，不是直接写入第三方音乐平台。
- 支持从 Plex 本地曲库试听可靠匹配结果；不会下载外部音频，试听不计入播放学习，并限制文件范围、请求大小与同时播放数量。
- 开启跟随更新后复用现有串行后台队列；来源失败会记录错误并保留上一次成功结果，不会用空结果覆盖已经发布的 Plex 歌单。
- 外部歌单刷新改为原子更新：Plex 暂时不可用时不覆盖旧快照和人工确认；来源大量删歌仍需单独确认，手动重试不会绕过这层保护。
- 已托管的 Plex 歌单若被手工删除，可在确认确实不存在后安全重建；网络超时不会被误判成删除，也不会重复创建同名歌单。
- 旧版数据可原地升级，原有 180 天、20,000 条播放学习记录、QQ 缓存、Plex Sonic 缓存、每日推荐和智能歌单均保持不变；仍有外部托管歌单的账户不能被误删。

## 1.2.1 - 2026-09-20

- 运行状态中的时间改为“最近活动”，每次有效的 Plex 播放、暂停、停止或听完事件处理后都会更新；不再只在形成新的偏好或冷却结论时变化。
- “学习中”只在当前确实存在播放会话时显示；歌曲停止或会话超时后恢复“已连接”，历史学习记录不会再让状态常驻。

## 1.2.0 - 2026-09-20

- 播放学习改为确认切歌后再判断：只有同一用户和播放器在 30 秒内开始另一首歌，才会把上一首计为跳过；退出、断网、暂停、拖动进度和同歌重播不会误伤。
- 同一用户使用多个音乐资料库时，即使个别 Plex 事件没有携带资料库编号，也只会在曲目归属唯一时补全并继续同一播放会话；无法唯一识别仍保持忽略，避免跨库学习。
- 每个“Plex 用户 · 音乐资料库”独立维护长期偏好、近期疲劳、个人跳歌基准与动态冷却；一次误操作不会永久排除歌曲，后续完整播放可以逐步恢复。
- 学习事件迁移到带索引的 SQLite 记录，每个项目保留 180 天、最多 20,000 条；事件与偏好汇总在同一事务写入，中断后可自动重建，降低 NAS 内存与磁盘写入。
- 每日推荐升级为 Daily Mix V2，按稳定偏好、近期口味、新鲜发现、久未重听、跨口味探索和恢复观察互斥选歌；新鲜比例会按实际收听自适应，未播放曲目最多连续保留两期，并执行 14/21 天防重复与歌手、专辑多样性限制。
- 新鲜歌曲比例只参考最近 60 次有效探索结果，旧口味不会长期压住近期变化；样本不足时保持中性的默认比例。
- 自动复用 Plex Sonic 相似歌曲结果并缓存 7 天；Plex Sonic 未完成、超时或不可用时自动退回曲库关系，不读取、上传或修改音频文件。
- 升级、切换曲库和删除项目时只处理目标项目的学习数据，不影响其他家庭成员、曲库、歌曲缓存和现有 Plex 歌单。
- 运行状态精简为“已学习、偏好歌曲、冷却中、当前播放”，临时疲劳不再显示为永久的负向歌曲。

## 1.1.9 - 2026-09-20

- 移除错误的 Webhook 6 小时过期判断；当前地址验证成功后保持“已接通”，只有地址密钥更新时才重新验证。
- 播放学习长时间没有新事件时仅表示暂时没有播放，不再误报连接失效，也不会干扰每日推荐状态。

## 1.1.8 - 2026-09-19

- 修复同一首歌在上一轮停止后再次播放时，已听完事件会错误清零“当前播放”的问题。

## 1.1.7 - 2026-09-19

- 播放达到 Plex 的已听完阈值时立即记录正向学习，但继续保持“当前播放”，直到真正停止、暂停或超时。
- Webhook 长时间没有收到新事件后显示“待验证”，不再把历史成功记录永久显示为“已连接”。

## 1.1.6 - 2026-09-19

- 修复完整曲库分析阶段的暂停按钮未通知逐曲扫描器，导致任务继续运行的问题。
- 暂停时保存原任务类型与逐曲进度；刷新页面、重启容器或点击继续后仍恢复正确任务，不会从头扫描。
- QQ 请求结束后立即响应暂停，暂停状态不再被错误覆盖成任务完成。

## 1.1.5 - 2026-09-19

- 修复自定义 `PORT` 下容器健康检查仍固定访问 9511，导致 TrueNAS 长时间显示部署中的问题。

## 1.1.4 - 2026-09-19

- 曲库整理改为先分析现有歌曲，再按实际可用曲目列出可创建的分类歌单；小型曲库最低 10 首、常规曲库按 0.5% 计算、大型曲库最高 30 首。
- 自动任务集中到“设置 → 自动任务”的三行面板，分别控制每日推荐、智能歌单和新增歌曲整理，可独立选择北京时间与更新周期。
- 自动任务对全部已启用的“Plex 用户 · 音乐资料库”生效，按新增歌曲整理、智能歌单、每日推荐串行执行；单个档案失败不会阻断其他档案。
- 移除每日推荐、智能歌单、曲库整理和批量用户区域的重复自动开关，保留全部手动生成、发布、删除、暂停和检查新增歌曲操作。
- 升级只迁移旧开关，不删除或移动既有缓存、播放学习记录、QQ 授权、托管歌单和多用户档案。
- 修复已有分类歌单时无法继续发现新分类、首次分析未续跑逐曲缓存、旧主题选择残留导致无候选的问题。
- 修复智能歌单重试饿死其他歌单、自动任务关闭后复用旧时段、手动预览导致当日自动更新被跳过的问题。
- 升级前生成的稀疏预览不能绕过新门槛写入；每日推荐进入安全恢复状态后，必须手动发布成功一次才恢复自动更新。

## 1.1.3 - 2026-09-19

- Plex 播放事件缺少资料库编号时，会用已缓存曲库唯一归属安全识别；无法唯一判断时保持忽略，避免串写学习记录。
- 添加家庭成员或共享用户时，按当前 Plex 服务器寻找对应所有者，不再错误使用列表中的第一个管理员账户。

## 1.1.2 - 2026-09-19

- 播放学习并入“账户与成员”，每个“用户 · 音乐资料库”独立开关；新添加项目默认开启，也可随时关闭且不删除已有记录。
- Webhook 设置改为全局入口，并修复关闭学习或切换用户后误报未接通的问题。
- 修复同一用户多个音乐库的播放事件路由，以及 Plex 云端账号与服务器本地播放账号不一致时无法读取历史的问题。
- 移除重复的播放学习账户选择与旧设置接口，切换学习状态时自动废弃已过期的推荐预览。

## 1.1.1 - 2026-09-19

- 账户与音乐资料库选择改为等宽对齐，用户操作按钮统一排列；切换用户后停留在当前页面。
- 批量预览改为持久恢复，离开设置页再返回仍可直接发布，并统一显示“用户 · 音乐资料库”。
- 新增“全部每日更新”开关，一次校验并原子更新所有已发布用户，避免部分用户设置成功、部分失败。

## 1.1.0 - 2026-09-19

- 同一个 Plex 用户可以为不同音乐资料库建立独立项目，分别保存设置、缓存、学习记录和托管歌单。
- 切换用户或资料库后，浏览器标签页会固定到对应项目，已发布的今日歌单不会再消失。
- 更新 Plex 授权时同步刷新运行凭据；已停用项目可由页面自动恢复到可用项目。
- 加强未完成发布、修复、回滚与资料库切换保护，避免把一个资料库的状态带到另一个资料库。

## 1.0.7 - 2026-09-19

- 修复多 Plex 档案并发切换时请求可能落到错误用户的问题，并统一后台任务准入。
- 修复关闭自动整理、每日推荐恢复、智能歌单顺序回读和分类歌单恢复等边界问题。
- 分类歌单删除与恢复增加账户、服务器、资料库、同名歌单和操作状态保护。
- 首次管理员注册改为原子写入，公开登录接口统一执行同源与请求大小检查。
- 曲库预览全部不可同步时直接显示阻止原因，不再留下空白结果区域。

## 1.0.6 - 2026-09-18

- 每日推荐只管理歌单成员，不再因部分 Plex 服务器不支持音频歌单重排而误报失败。
- 发布与安全修复统一校验歌曲无重复、无缺失、无额外成员；Plex 返回顺序不影响使用。

## 1.0.5 - 2026-09-18

- 每日推荐重排改为按 Plex 官方客户端的方式连续移动，最后统一回读确认。
- 延长 Plex 写入后的只读确认窗口，避免服务器短暂返回旧顺序时误报失败。

## 1.0.4 - 2026-09-18

- 自动更新开关移到每日推荐页顶部，自动整理开关移到曲库概况顶部。
- 曲库整理恢复持续显示的进度条和可用的暂停按钮，结果表只保留歌单、歌曲和新增数量。
- 修复 Plex 写入成功后短暂回读旧歌单而被误判为发布失败的问题。

## 1.0.3 - 2026-09-18

- 精简每日推荐、曲库整理、Plex 连接与播放学习界面，移除重复说明和重复状态。
- 播放学习会在只有一个 Plex 用户时自动选中；多用户未选择时明确停在“选择账户”。
- 音乐资料库改为选择后自动保存，批量推荐仅在存在多个用户时显示。

## 1.0.2 - 2026-09-18

- 简化每日推荐：首次只显示“生成今日歌单”，生成后只保留“换一批”和“发布到 Plexamp”。
- 发布时直接创建或更新 Plex 中固定的“每日推荐”歌单，不再因同名歌单阻止发布。
- 后续手动与自动更新始终复用同一张歌单，不再展示内部检查和历史降级术语。

## 1.0.1 - 2026-09-18

- 修复全新安装没有主题选项、无法创建第一批分类歌单的问题。
- 全新用户可在没有任何历史缓存或来源记录时选择内置主题、生成预览并确认同步。
- 首次整理会按已选主题建立真实 QQ 分类来源；没有可靠来源的主题会明确标为不可用。

## 1.0.0 - 2026-09-18

- 首个公开稳定版本。
- 提供每日推荐、智能歌单、多用户播放学习、儿歌隔离与 QQ 音乐资料增强。
- 全新安装可直接建立管理员账户；Plex 官方授权完成后会自动恢复并继续连接。
- 支持 Docker、TrueNAS SCALE、AMD64 与 ARM64。
- 提供歌曲资料缓存的校验、导出和导入工具。
- 所有歌单写入均经过预览、确认和助手所有权保护。
