# 更新记录

## 2.0.0 - 2026-09-22

- 顶部导航、主题和退出收进同一个“菜单”，三种主题仍可随时切换；窄屏与深色模式沿用同一套交互。
- 导入歌单的链接、来源、Plex 歌单和歌曲结果采用统一宽度与边界，消除表单和列表的错位；播放器音量图标作轻微视觉居中调整。
- 本次只调整界面和版本标识，不迁移歌单、账户或播放数据。NAS 内存占用将以发布前后的实际读数核对，不把样式改动当作已证实的内存原因。

## 1.4.25 - 2026-09-22

- 保留 Plex 风格的四张封面拼图；歌单侧栏与智能歌单卡片改为封面在左、名称及歌曲数在右，窄屏同步收紧尺寸，避免封面压在文字上方。
- 深色主题补齐下拉选项、禁用选项、整理步骤、状态标签、账户头像、设置提示与导入页警告的背景和文字配色，改善浅色弹层与深色文字混用导致的辨识问题。
- 本次只调整界面样式，不迁移歌单或播放数据。

## 1.4.24 - 2026-09-22

- 歌单侧栏、分类卡片和歌单标题可显示当前 Plex 曲库的歌曲封面拼图；缺少封面时使用简洁占位，切换用户后不会混用其他曲库的图片。
- 右上角新增衣服图标，可在清爽浅色、暖色纸感和深色夜间三种主题之间切换；选择只保存在当前浏览器，字体与页面结构保持不变。

## 1.4.23 - 2026-09-22

- 新增用户的每日推荐默认 50 首；已保存的个人数量保持不变。设置页移除重复的“推荐规则”，仍可在“智能歌单 → 每日推荐”中调整。
- 顶部全库搜索靠左并统一输入框样式；导入歌单列表移除没有含义的 `#` 表头，序号与待确认勾选列保留。
- 收紧播放学习的账户与曲库边界：Plex Webhook 缺少曲库编号时，只有曲目在唯一曲库目录中得到验证才记录；历史记录缺少可验证用户编号时不用于推荐，旧的未验证历史缓存不再复用。关闭学习仍保留已有偏好，只停止收集新的播放证据。

## 1.4.22 - 2026-09-22

- 修复同一 Plex 用户连接多个音乐曲库时，另一个曲库已由本程序托管的同名智能歌单被误判为冲突，导致新档案的「每周常听」「时光胶囊」生成需处理。仅放行能验证归属、资料库和内容状态的程序歌单，手工歌单仍不接管或覆盖。
- 新用户初始化任务完成后，已打开的设置页会自动更新状态，无需手动刷新。

## 1.4.21 - 2026-09-22

- 移除用户后，设置页会自动追踪清理结果；完成后无需刷新网页即可更新列表，仍需处理的状态会保留原因。
- 添加用户按“Plex 用户＋音乐资料库”选择：管理员与已添加过的成员继续显示，可为同一用户添加其他曲库；已添加或待清理的同一组合不可重复添加。
- 新增设置页交互回归测试，核对多曲库添加与移除状态自动更新。

## 1.4.20 - 2026-09-22

- 一个 Plex 用户与一个音乐曲库构成独立档案；新增档案只生成自己的每日推荐、每周常听、时光胶囊和最近新增，同曲库已整理的分类歌单直接同步，不重新扫描曲库。
- 主账户分类歌单的创建、修改和删除会按曲库同步到对应用户；移除档案时只清理由本程序核实创建的歌单，Plex 自建歌单与音乐文件不受影响。删除结果不确定时保留档案供核对和重试。
- 设置页集中显示播放学习、每日推荐、智能歌单三个独立开关，统一对齐并减少重复文字；新增用户失败状态可就地重试，新增用户不会擅自切换当前账户。
- 升级时保留现有档案、歌单和已生效的自动更新状态。部署前请备份 `/data`；关闭开关只停止后续更新，不会删除现有歌单。

## 1.4.19 - 2026-09-22

- 设置页恢复明确的 Plex 用户选择；音乐资料库改为选择后点击“保存”才切换，避免误操作。
- 移除过的用户重新添加时显示“添加”，创建全新的本地档案，不恢复旧播放记录和管理状态；旧档案与 Plex 歌单保留，若旧歌单仍占用目标则阻止重复创建。
- 每日推荐区分全部账户的执行计划和各账户的自动更新开关；全局设置不再覆盖个别账户的关闭状态。安全暂停显示原因，需手动预览、发布核对后才可重新开启。
- 每日推荐用户列表的播放学习、每日更新及移除操作对齐，管理员行保留空操作位。

## 1.4.18 - 2026-09-22

- 导入歌单的“试听”按钮保持原文案，只在当前播放项旁显示小喇叭；切换分页后恢复播放标记，暂停或失败时清除。
- 试听歌曲把 Plex 曲目编号、实际时长、所属账户和喜欢状态传给底部播放器，爱心可直接操作且状态回传到导入页；进度条扩大拖动区域，拖完一次提交，无法原生定位的音频改用 Plex 偏移转码定位。
- “自动刷新”补充悬停说明：随新增歌曲整理运行时重新读取来源，立即读取仍使用“重新读取”。

## 1.4.17 - 2026-09-21

- 发布后复查发现，内嵌设置页与歌单侧栏各有一个账户切换框。内嵌模式只保留侧栏的切换框；独立打开设置页时仍保留自己的切换框。

## 1.4.16 - 2026-09-21

- 复查设置、导入和曲库整理页，删去设置页底部与顶栏和侧栏重复的入口及版本；补歌导出行和整理任务标题也不再重复附近的标签或操作名。
- 修复在内嵌设置页切换或新增 Plex 用户后外层歌单列表仍停留在旧用户的问题；外层先核验用户列表，只接受最新一次选择，再切换并刷新歌单。
- 导入页改填歌单链接或完成导入后，清除失效的文件选择提示，避免界面同时显示链接与旧文件名。
- 修复 701–720px 窄屏上全库搜索结果的喜欢和添加按钮被隐藏的问题。

## 1.4.15 - 2026-09-21

- 待确认歌曲改为选候选或“标记为缺失”后自动勾选，只在顶部统一确认；无候选时也能标记缺失，全选仍要求逐首确定处理方式。
- 下载 TXT、CSV、长图的菜单项统一对齐和点击区域，点击空白处或按 Esc 可收起；导入控制区两侧留白一致，不再将操作推到页面边缘。
- 设置页收窄为居中的单列，去掉重复的账户标题；推荐规则、自动任务和系统设置保留可见，旧设置链接仍能定位对应内容。
- 恢复“复制地址”旁的“打开 Webhook”，直接在新标签页进入 Plex 账户的 Webhooks 设置。
- 用户列表移除重复的“打开/当前”按钮；账户切换统一使用侧栏账户选择器，仍保留播放学习和移除用户操作。设置页也不再重复列出左侧已有的歌单入口。
- 修复 Plex 曲库在批量确认前变化时使用旧待确认列表的问题，重新匹配后再核验所选歌曲。

## 1.4.14 - 2026-09-21

- 程序维护的评分收藏歌单改名为“我喜欢”，每个 Plex 用户及音乐曲库独立建立；Plex 里原有的“我的最爱”继续作为单独歌单显示，不按名称接管、修改或删除。
- 修复 Plex 将智能歌单规则回读为 `library://` 地址时的误判；只接受当前曲库的纯评分规则，程序创建的歌单以独立编号记录，无法核验时停止写入。
- 侧栏只显示歌单名和歌曲数，不再把笼统的“未同步 Plex”或“智能”标记贴在歌单下方；同步失败仍会明确提示原因。

## 1.4.13 - 2026-09-21

- 导入歌单的匹配结果改成固定列歌曲列表；待确认歌曲可逐首选候选、全选并一次确认，批量操作在校验全部选择后才写入，避免半途改动。
- 缺失歌曲只显示已有的歌曲信息，移除外部平台搜索入口；缺失清单仅保留一处复制操作，并保留 TXT、CSV 和长图导出。
- 导入、智能歌单、曲库整理、设置与运行状态等页头移除重复标题、返回按钮和解释文字；控制项收拢到内容宽度内，窄屏自动换行。

## 1.4.12 - 2026-09-21

- 自建歌单侧栏移除没有功能的音乐符号，歌单名和歌曲数恢复为紧凑的上下两行，不再被图标挤开。
- 歌曲列表、搜索结果和播放器的爱心不再使用通用提交中转圈样式；点击后立即显示红心或空心，请求失败会恢复原状态并提示。
- 曲库整理页按简洁布局重排：主操作移到标题旁，概况、自动整理和分类歌单依次呈现；歌单名可直接打开，停止维护和移除仍留在更多菜单并保留确认保护。
- 导入页限制内容宽度、增加统一内边距，将导入、已导入、Plex 歌单和缺失清单的标签、内容及操作对齐到同一网格；窄屏操作会换行，不再把字贴边或把按钮推到浏览器最右侧。

## 1.4.11 - 2026-09-21

- “我的最爱”按每个 Plex 用户和音乐曲库独立显示；五星制的 4～5 星、Plexamp 单星模式已点亮的歌曲都纳入。进入曲库时尝试建立对应的 Plex 智能歌单；能确认是旧的纯五星规则时原位升级，不删除歌单。遇到同名冲突、额外规则或回读不一致时停止修改并提示核对。
- 导入歌单页改为单列单滚动，导入、来源下拉切换、Plex 歌单更新和缺失清单操作统一置于歌曲列表上方；侧栏增加“导入歌单”入口，发布后的入口会刷新。
- 每日推荐发布时间与规则放在一起，其他智能歌单的更新时间单独放在下方，并支持每天更新；修复调整歌曲数或天数时面板意外收起。
- 智能生成及曲库整理歌单不再列为手动加歌目标；新建普通歌单直接在 Plex 创建，无需再次发布。分类歌单页精简重复标题和按钮。
- 顶栏搜索框、播放器音量按钮及爱心尺寸和悬停状态统一调整；播放进度条显示已播放的颜色段，拖动时同步更新。

升级说明：旧“我的最爱”只有在规则可验证为纯五星筛选时才原位迁移；同名普通歌单和复杂规则不会自动覆盖。首次打开每个曲库会尝试为该 Plex 用户建立自己的智能歌单，若该用户没有创建权限，网页收藏仍可使用，界面会提示 Plex 端暂未同步。

## 1.4.10 - 2026-09-21

- 修复 Plex 创建空白自建歌单时返回 400、网页误报 500：先从所选曲库临时加入一首歌，验证后立即清空并回读；结果不确定时提示歌单编号，不盲目重试。
- 合并网页爱心与 Plex 四星、五星的“我的最爱”视图；隐藏重复的 Plex 智能歌单，包括旧缓存回退时。原歌单不删除。
- 播放器音量入口移到中央播放控制旁，进度条拖动后的焦点边框改为细线样式。
- 已连接的用户和音乐库默认首次生成每日推荐并按北京时间每天更新；网页删除后不自动重建，明确关闭后也保持关闭。同一 Plex 账户的多个音乐库使用独立每日推荐名称，并在发布前再次核对归属。

## 1.4.9 - 2026-09-21

- 主账户维护的同库曲库整理歌单默认同步给 Plex 家庭或分享用户，更新或删除分类也会同步；沿用主账户的 QQ 匹配结果，分享用户无需再次授权 QQ。个人在 Plex 删除自己的同步副本即可退出该歌单，管理员可从该用户的曲库整理页恢复同步；不会接管同名或手动改过的歌单。
- Plex 原生歌单、全库搜索与网页播放不再要求先建立本地歌曲缓存；播放和爱心写入仍逐曲校验当前 Plex 音乐库，跨库歌曲不会误播或误改。网页播放学习也接受经 Plex 验证的新入库歌曲。
- 智能歌单、曲库整理各自在首页提供就近的设置入口；每日推荐规则与其他智能歌单、自动更新时间集中管理，全局设置收起重复的歌单与任务栏目。生成与删除仅作用于对应歌单。
- 歌曲列表、搜索结果和底部播放器的爱心统一改为歌名旁的两态操作；新收藏使“我的最爱”出现按用户区分的红点，进入后消除。播放器进度线改细，保留拖动区域。
- 外部导入页把二次导入收为入口、匹配结果独立滚动并分页；自建歌单标题旁不再显示易误解的总数。升级不删除原有歌单或音乐文件。
- 每日推荐发布前可展开核对前 10 首；Plex 歌单缺少曲库或评分字段时安全核对、回退，未同步的“我的最爱”不再误显示为 0 首。
- 顶部和手机侧栏的“设置”与“运行状态”统一在播放器中间打开，保留侧栏、搜索和底部播放；内嵌设置中的站内链接也不再使播放器整页跳转。

## 1.4.8 - 2026-09-21

- 大歌单歌曲行改为分批显示；播放状态和爱心变化只更新已显示的行，不再一次性重建数千首歌，快速切换时保持页面响应。
- 设置页作为独立管理入口打开，内部的每日推荐、智能歌单和曲库整理链接不再被嵌入框架拦截；播放页的按钮明确标示为当前歌单的快捷调整。
- 自建歌单侧栏将“新建”与“外部导入”分开；可在网页中创建空白 Plex 普通歌单，随后搜索并添加本地曲库歌曲。创建结果不明时不会自动重试，删除后清理本地归属记录。
- 页面必需的脚本、样式、图标与封面继续由本应用或本地 Plex 提供，移除设置中依赖 Plex 网站的可选跳转；保留局域网手动连接 Plex 的途径。
- HTTPS 反代配置兼容独立域名与原有局域网入口，按访问来源设置安全会话 Cookie，并补充根路径、请求头、音频 Range 和缓存要求。没有自动开启反向代理。

## 1.4.7 - 2026-09-21

- 修复 Plex 智能歌单歌曲缺少 `playlistItemID` 时误报重复并返回 500 的问题；当前曲库的智能歌单可正常打开。
- 侧栏仅显示来源可确认属于当前音乐库的 Plex 智能歌单；其他曲库及已归档档案托管的歌单只从当前视图隐藏，不删除 Plex 歌单或音乐文件，并阻止跨库误改。
- 同一 Plex 账户、同一服务器上已启用的音乐库共享网页端“我的最爱”；跨库爱心、播放与播放记录按歌曲所属曲库处理。
- “设置”新增“歌单与曲库”，可直接进入每日推荐、智能歌单和曲库整理；歌单卡片悬停改为清晰的浅底深字。
- 升级不迁移或删除现有数据。若 Plex 中旧智能歌单仍引用已移除的音乐库，它们保留在 Plex，但不再混入当前音乐库侧栏。

## 1.4.6 - 2026-09-21

- “我的歌单”改为简洁的 QQ 音乐式导航：左侧固定“智能歌单”和“曲库整理”，下方集中显示 Plex / Plexamp 自建歌单与外部导入歌单，新建曲库和原有 Plex 歌单都会按当前账户完整展示。
- 新增“我的最爱”和统一爱心操作；点亮或取消爱心会同步写入 Plex 的五星或零星评分，原有四星、五星歌曲自动归入“我的最爱”，搜索结果、歌曲列表和播放器状态保持一致。
- Plex / Plexamp 普通歌单支持网页端重命名、添加歌曲、移除歌曲和删除歌单；智能歌单保留重命名与删除，成员继续由 Plex 规则维护。删除操作只删除歌单，不删除音乐文件。
- 修复每日推荐等歌单无法播放、搜索歌曲拖动后中断的问题；浏览器直接播放可安全定位的 FLAC，所有本地曲库入口统一使用同一条受保护音频路径。
- 移除含义不清的“搜索全库添加”独立按钮，保留顶栏全库搜索；修正弹窗关闭按钮在未悬停时看不清，以及切换账户、加载失败和连续爱心操作的状态边界。

## 1.4.5 - 2026-09-21

- FLAC 网页播放改用曲库元数据驱动总时长和进度，拖动时可按秒从 Plex 转码流的新位置继续播放；切歌、重试与旧来源事件不再污染当前时间线。
- 音频代理在浏览器中断或换源时立即释放流和并发槽，并为快速切歌保留有限余量，降低每日推荐及其他歌单的偶发播放失败。
- 静音按钮新增清晰叉号，声音状态、图标、标题与无障碍标签保持同步。
- “添加歌曲”入口改为“搜索全库添加”，点击后聚焦全库搜索并明确提示如何把结果加入歌单。
- 网页播放器按当前 Plex 档案上报播放、进度、暂停、停止与听完事件，复用既有播放学习规则更新最近活动，且包含参数校验和幂等保护。

## 1.4.4 - 2026-09-20

- 修复“新建智能歌单”“导入外部歌单”“曲库整理”在统一工作区打开后右侧空白的问题，并保持返回歌单和跨页面播放状态。
- 歌单切换不再把加载转圈插入侧栏按钮，避免切换时图标放大、文字移位和类似整页刷新的闪烁。
- 底部播放器改为顶部进度条、中央播放控制、右侧时间与紧凑音量入口；统一使用 SVG 图标，上一首和下一首悬停时不再变成第二个绿色主按钮。
- 歌曲列表恢复独立的歌曲、歌手、专辑和时长列，并补齐中等宽度与窄屏布局，防止右侧内容被裁切。
- 音量浮层同时支持鼠标悬停和键盘聚焦；音量归零后可一键恢复最近的非零音量，静音状态与辅助说明保持一致。

## 1.4.3 - 2026-09-20

- “我的歌单”改为真正的全屏音乐工作区：顶栏、歌单侧栏、内容标题与底部播放器保持固定，只有歌单列表、歌曲列表和内嵌工具内容各自滚动。
- 顶栏改为全库搜索，可直接播放搜索结果并加入已有受管歌单；歌单、搜索、创建工具、运行状态和设置在同一页面切换，播放不会因切换界面中断。
- 播放器拆成独立模块并取消音频元数据预加载，避免一次点击触发相邻的重复 Plex 转码请求；首次启动失败会自动重试一次，仍失败才在底栏显示操作。
- 播放请求使用代际标识隔离，快速换歌时上一首迟到的失败不会覆盖当前歌曲；Plex 存在多个安全音频版本时优先使用选中版本，否则按服务器顺序稳定选择。
- 工作区、全库搜索和播放器分别由独立模块维护，页面入口只负责账户与歌单编排；视觉尺寸全部使用语义变量，为后续壁纸和皮肤切换保留统一接口。

## 1.4.2 - 2026-09-20

- “我的歌单”改为固定账户与工具区、独立滚动歌单区和紧凑歌曲列表；歌单切换保持页面尺寸稳定，底部播放器缩为单行并在原处显示失败与重试。
- 修复切换 Plex 用户或音乐资料库后仍显示上一个账户歌曲的问题；快速连续切换也会丢弃过期响应，不再串号。
- 播放队列与当前浏览歌单分离，切换歌单、进入创建工具或删除其他歌单时不再误停正在播放的歌曲。
- 浏览器无法直接播放的 FLAC 等格式由 Plex 实时转为 MP3，原始音乐文件不修改；封面、歌名、歌手、进度、音量与连续播放统一保留在底栏。
- 每日推荐在首次发布前也保留唯一入口，不再同时显示两个“每日推荐”，也不会因尚未创建而从首页消失。
- 外部歌单整理为紧凑导入栏、来源切换和结果区，嵌入首页时自动适应内容高度，试听失败显示在对应歌曲旁边。

## 1.4.1 - 2026-09-20

- 修复恢复数据后安装标识变化导致旧版受管歌单被误报“助手管理标记缺失”；只有歌单编号、名称、成员和已保存指纹全部一致时才迁移标记，普通同名歌单仍不会被接管。
- 首页会先显示完整歌单列表，再逐个打开可用歌单；某一个旧歌单需要核对时，不再导致侧栏数量归零或遮住其他正常歌单。
- 每日推荐、智能歌单、分类歌单与外部导入歌单统一使用恢复后的所有权校验；标记迁移只更新摘要，不改变歌曲、顺序、封面或音乐文件。

## 1.4.0 - 2026-09-20

- 首页升级为统一“我的歌单”：每日推荐、智能歌单、分类歌单和外部歌单按当前 Plex 用户与音乐库集中展示，不再分散到多个顶层页面。
- 新增连续播放队列和固定底部播放器，可从歌单任意歌曲开始播放，自动续播下一首，并直接显示 Plex 曲库已有封面、歌名、歌手和进度。
- 支持搜索当前 Plex 曲库并把歌曲加入任一受管歌单，也可从歌单手动移除歌曲；个人增删会作为独立调整保存，后续自动更新继续尊重这些选择。
- 每个试听与封面请求都会重新校验当前用户、音乐库、受管歌单及曲目归属；浏览器不会取得 Plex Token，也不能借接口读取歌单之外的音频。
- 每日推荐、智能歌单、外部歌单和曲库整理集中到首页侧栏的“创建与整理”，在同一工作区打开，播放不会因进入创建页面而中断。
- 外部歌单页面精简为“导入歌单、创建 Plex 歌单、缺失歌曲”三块；试听控制和进度回到歌曲行内，不再需要滚到页面底部暂停。

## 1.3.1 - 2026-09-20

- 修复 QQ 音乐客户端生成的 `c6.y.qq.com` 公开分享短链接无法导入；现在会在逐跳校验 HTTPS、公网 DNS 和 QQ 官方域名后解析真实歌单编号，其他跳转仍会拒绝。

## 1.3.0 - 2026-09-20

- 新增“外部歌单”：支持 QQ 音乐公开歌单、QQ 排行榜、网易云音乐公开歌单，以及 M3U、TXT、CSV 文件；导入结果按 Plex 用户和音乐资料库独立保存。
- 使用保守的多阶段匹配，分开展示可靠匹配、需要确认和曲库缺失；只有可靠匹配或用户确认的本地歌曲才可发布，避免同名、翻唱、现场版和多歌手作品误配。
- 外部歌单发布沿用助手所有权保护，只更新本软件创建的对应 Plex 歌单；修改、删除和来源更新均校验账户、服务器、曲库与歌单身份，不碰用户手工歌单。
- 缺失歌曲可按 QQ 音乐或网易云音乐格式导出，方便复制到官方客户端搜索和补歌；这只是生成清单，不是直接写入第三方音乐平台。
- 支持从 Plex 本地曲库试听可靠匹配结果；不会下载外部音频，试听不计入播放学习，并限制文件范围、请求大小与同时播放数量。
- 开启跟随更新后复用现有串行后台队列；来源失败会记录错误并保留上一次成功结果，不会用空结果覆盖已经发布的 Plex 歌单。
- 外部歌单刷新改为原子更新：Plex 暂时不可用时不覆盖旧快照和人工确认；来源大量删歌仍需单独确认，手动重试不会绕过这层保护。
- 已托管的 Plex 歌单若被手工删除，可在确认确实不存在后安全重建；网络超时不会被误判成删除，也不会重复创建同名歌单。
- 旧版数据可原地升级，原有 180 天、20,000 条播放学习记录、QQ 缓存、Plex Sonic 缓存、每日推荐和智能歌单均保持不变；仍有外部托管歌单的账户不能被误删。

## 1.2.1 - 2026-09-20

- 运行状态中的时间改为“最近活动”，每次有效的 Plex 播放、暂停、停止或听完事件处理后都会更新；不再只在形成新的偏好或冷却结论时变化。
- “学习中”只在当前确实存在播放会话时显示；歌曲停止或会话超时后恢复“已连接”，历史学习记录不会再让状态常驻。

## 1.2.0 - 2026-09-20

- 播放学习改为确认切歌后再判断：只有同一用户和播放器在 30 秒内开始另一首歌，才会把上一首计为跳过；退出、断网、暂停、拖动进度和同歌重播不会误伤。
- 同一用户使用多个音乐资料库时，即使个别 Plex 事件没有携带资料库编号，也只会在曲目归属唯一时补全并继续同一播放会话；无法唯一识别仍保持忽略，避免跨库学习。
- 每个“Plex 用户 · 音乐资料库”独立维护长期偏好、近期疲劳、个人跳歌基准与动态冷却；一次误操作不会永久排除歌曲，后续完整播放可以逐步恢复。
- 学习事件迁移到带索引的 SQLite 记录，每个项目保留 180 天、最多 20,000 条；事件与偏好汇总在同一事务写入，中断后可自动重建，降低 NAS 内存与磁盘写入。
- 每日推荐升级为 Daily Mix V2，按稳定偏好、近期口味、新鲜发现、久未重听、跨口味探索和恢复观察互斥选歌；新鲜比例会按实际收听自适应，未播放曲目最多连续保留两期，并执行 14/21 天防重复与歌手、专辑多样性限制。
- 新鲜歌曲比例只参考最近 60 次有效探索结果，旧口味不会长期压住近期变化；样本不足时保持中性的默认比例。
- 自动复用 Plex Sonic 相似歌曲结果并缓存 7 天；Plex Sonic 未完成、超时或不可用时自动退回曲库关系，不读取、上传或修改音频文件。
- 升级、切换曲库和删除项目时只处理目标项目的学习数据，不影响其他家庭成员、曲库、歌曲缓存和现有 Plex 歌单。
- 运行状态精简为“已学习、偏好歌曲、冷却中、当前播放”，临时疲劳不再显示为永久的负向歌曲。

## 1.1.9 - 2026-09-20

- 移除错误的 Webhook 6 小时过期判断；当前地址验证成功后保持“已接通”，只有地址密钥更新时才重新验证。
- 播放学习长时间没有新事件时仅表示暂时没有播放，不再误报连接失效，也不会干扰每日推荐状态。

## 1.1.8 - 2026-09-19

- 修复同一首歌在上一轮停止后再次播放时，已听完事件会错误清零“当前播放”的问题。

## 1.1.7 - 2026-09-19

- 播放达到 Plex 的已听完阈值时立即记录正向学习，但继续保持“当前播放”，直到真正停止、暂停或超时。
- Webhook 长时间没有收到新事件后显示“待验证”，不再把历史成功记录永久显示为“已连接”。

## 1.1.6 - 2026-09-19

- 修复完整曲库分析阶段的暂停按钮未通知逐曲扫描器，导致任务继续运行的问题。
- 暂停时保存原任务类型与逐曲进度；刷新页面、重启容器或点击继续后仍恢复正确任务，不会从头扫描。
- QQ 请求结束后立即响应暂停，暂停状态不再被错误覆盖成任务完成。

## 1.1.5 - 2026-09-19

- 修复自定义 `PORT` 下容器健康检查仍固定访问 9511，导致 TrueNAS 长时间显示部署中的问题。

## 1.1.4 - 2026-09-19

- 曲库整理改为先分析现有歌曲，再按实际可用曲目列出可创建的分类歌单；小型曲库最低 10 首、常规曲库按 0.5% 计算、大型曲库最高 30 首。
- 自动任务集中到“设置 → 自动任务”的三行面板，分别控制每日推荐、智能歌单和新增歌曲整理，可独立选择北京时间与更新周期。
- 自动任务对全部已启用的“Plex 用户 · 音乐资料库”生效，按新增歌曲整理、智能歌单、每日推荐串行执行；单个档案失败不会阻断其他档案。
- 移除每日推荐、智能歌单、曲库整理和批量用户区域的重复自动开关，保留全部手动生成、发布、删除、暂停和检查新增歌曲操作。
- 升级只迁移旧开关，不删除或移动既有缓存、播放学习记录、QQ 授权、托管歌单和多用户档案。
- 修复已有分类歌单时无法继续发现新分类、首次分析未续跑逐曲缓存、旧主题选择残留导致无候选的问题。
- 修复智能歌单重试饿死其他歌单、自动任务关闭后复用旧时段、手动预览导致当日自动更新被跳过的问题。
- 升级前生成的稀疏预览不能绕过新门槛写入；每日推荐进入安全恢复状态后，必须手动发布成功一次才恢复自动更新。

## 1.1.3 - 2026-09-19

- Plex 播放事件缺少资料库编号时，会用已缓存曲库唯一归属安全识别；无法唯一判断时保持忽略，避免串写学习记录。
- 添加家庭成员或共享用户时，按当前 Plex 服务器寻找对应所有者，不再错误使用列表中的第一个管理员账户。

## 1.1.2 - 2026-09-19

- 播放学习并入“账户与成员”，每个“用户 · 音乐资料库”独立开关；新添加项目默认开启，也可随时关闭且不删除已有记录。
- Webhook 设置改为全局入口，并修复关闭学习或切换用户后误报未接通的问题。
- 修复同一用户多个音乐库的播放事件路由，以及 Plex 云端账号与服务器本地播放账号不一致时无法读取历史的问题。
- 移除重复的播放学习账户选择与旧设置接口，切换学习状态时自动废弃已过期的推荐预览。

## 1.1.1 - 2026-09-19

- 账户与音乐资料库选择改为等宽对齐，用户操作按钮统一排列；切换用户后停留在当前页面。
- 批量预览改为持久恢复，离开设置页再返回仍可直接发布，并统一显示“用户 · 音乐资料库”。
- 新增“全部每日更新”开关，一次校验并原子更新所有已发布用户，避免部分用户设置成功、部分失败。

## 1.1.0 - 2026-09-19

- 同一个 Plex 用户可以为不同音乐资料库建立独立项目，分别保存设置、缓存、学习记录和托管歌单。
- 切换用户或资料库后，浏览器标签页会固定到对应项目，已发布的今日歌单不会再消失。
- 更新 Plex 授权时同步刷新运行凭据；已停用项目可由页面自动恢复到可用项目。
- 加强未完成发布、修复、回滚与资料库切换保护，避免把一个资料库的状态带到另一个资料库。

## 1.0.7 - 2026-09-19

- 修复多 Plex 档案并发切换时请求可能落到错误用户的问题，并统一后台任务准入。
- 修复关闭自动整理、每日推荐恢复、智能歌单顺序回读和分类歌单恢复等边界问题。
- 分类歌单删除与恢复增加账户、服务器、资料库、同名歌单和操作状态保护。
- 首次管理员注册改为原子写入，公开登录接口统一执行同源与请求大小检查。
- 曲库预览全部不可同步时直接显示阻止原因，不再留下空白结果区域。

## 1.0.6 - 2026-09-18

- 每日推荐只管理歌单成员，不再因部分 Plex 服务器不支持音频歌单重排而误报失败。
- 发布与安全修复统一校验歌曲无重复、无缺失、无额外成员；Plex 返回顺序不影响使用。

## 1.0.5 - 2026-09-18

- 每日推荐重排改为按 Plex 官方客户端的方式连续移动，最后统一回读确认。
- 延长 Plex 写入后的只读确认窗口，避免服务器短暂返回旧顺序时误报失败。

## 1.0.4 - 2026-09-18

- 自动更新开关移到每日推荐页顶部，自动整理开关移到曲库概况顶部。
- 曲库整理恢复持续显示的进度条和可用的暂停按钮，结果表只保留歌单、歌曲和新增数量。
- 修复 Plex 写入成功后短暂回读旧歌单而被误判为发布失败的问题。

## 1.0.3 - 2026-09-18

- 精简每日推荐、曲库整理、Plex 连接与播放学习界面，移除重复说明和重复状态。
- 播放学习会在只有一个 Plex 用户时自动选中；多用户未选择时明确停在“选择账户”。
- 音乐资料库改为选择后自动保存，批量推荐仅在存在多个用户时显示。

## 1.0.2 - 2026-09-18

- 简化每日推荐：首次只显示“生成今日歌单”，生成后只保留“换一批”和“发布到 Plexamp”。
- 发布时直接创建或更新 Plex 中固定的“每日推荐”歌单，不再因同名歌单阻止发布。
- 后续手动与自动更新始终复用同一张歌单，不再展示内部检查和历史降级术语。

## 1.0.1 - 2026-09-18

- 修复全新安装没有主题选项、无法创建第一批分类歌单的问题。
- 全新用户可在没有任何历史缓存或来源记录时选择内置主题、生成预览并确认同步。
- 首次整理会按已选主题建立真实 QQ 分类来源；没有可靠来源的主题会明确标为不可用。

## 1.0.0 - 2026-09-18

- 首个公开稳定版本。
- 提供每日推荐、智能歌单、多用户播放学习、儿歌隔离与 QQ 音乐资料增强。
- 全新安装可直接建立管理员账户；Plex 官方授权完成后会自动恢复并继续连接。
- 支持 Docker、TrueNAS SCALE、AMD64 与 ARM64。
- 提供歌曲资料缓存的校验、导出和导入工具。
- 所有歌单写入均经过预览、确认和助手所有权保护。
