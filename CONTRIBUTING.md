# 参与贡献

提交改动前请先说明用户场景，保持普通页面简洁，并遵守以下规则：

1. 不提交 Token、Cookie、数据库、日志或真实播放历史。
2. 修复缺陷时先添加能够复现问题的测试。
3. 新的推荐来源必须接入统一候选、去重、过滤、预览和发布流程。
4. 所有写入 Plex 的操作必须保留预览、归属检查和防覆盖保护。
5. 运行 `PYTHONPATH=src python -m unittest discover -s tests -q` 后再提交。
