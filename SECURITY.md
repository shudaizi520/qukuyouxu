# 安全说明

请不要在公开 Issue、讨论区或截图中提交 Plex Token、QQ Cookie、管理员会话、数据库、备份文件或完整日志。

本应用默认面向家庭局域网，不应把管理端口直接暴露到公网。远程访问请使用可信 VPN，或放在启用 HTTPS 和额外访问控制的反向代理后面。

新安装首次打开时直接创建管理员用户名和密码，不需要设置码。应用面向可信家庭局域网：尚未创建账户时，第一位完成注册的人会成为管理员，因此请在安装完成后立即注册。使用 HTTPS 反向代理时必须配置 `PUBLIC_ORIGIN`，其值应与浏览器地址的协议和域名一致。

报告安全问题时，只描述复现条件和受影响版本，并先对所有凭据进行脱敏。GitHub 仓库启用 Private vulnerability reporting 后，请优先使用该入口；启用前不要在公开 Issue 中发布可利用细节。

如果凭据已经意外公开，请立即在对应服务中撤销或重新授权；删除帖子不能让已经泄露的凭据重新安全。
